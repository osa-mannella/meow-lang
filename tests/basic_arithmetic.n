// Basic arithmetic operations test
let a = 5
let b = 3
let sum = a + b
let diff = a - b
let product = a * b
let quotient = a / b
let result = sum + diff + product + quotient

// Negative numbers test
let negative = -5
let negative_expr = -a
let complex_neg = -(a + b)
